/**
 * Exercise importer entrypoint (spec §4, §78 `pnpm exercises:import`).
 *
 * Phase 1 stub — the real importer (Workout Guide adapter -> exercise DB) is
 * Phase 3 and depends on G-1 schema + G-3 provider abstraction. Kept as an
 * honest no-op so the wired command exists and runs (Node 24 strips TS types).
 */
function main(): void {
  console.log("[exercises:import] not implemented yet (Phase 3).");
}

main();
